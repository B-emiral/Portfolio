# profiles.toml
# TODO: fuse under single profile name
[dev]
llm_provider = "anthropic"
llm_model = "claude-3-5-haiku-20241022"
hookset_before = [
  "hooks.log_request"
]
hookset_after = [
  "hooks.mongo_insert",
  "hooks.langfuse_track",
  "hooks.guard_output"
]



[test]
llm_provider = "anthropic"
llm_model = "claude-3-5-sonnet-20241022"
hookset_before = [
  "hooks.log_request"
]
hookset_after = [
  "hooks.langfuse_track",
  "hooks.guard_output",
]





